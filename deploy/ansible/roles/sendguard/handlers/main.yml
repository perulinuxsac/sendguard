---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart sendguard-agent
  ansible.builtin.systemd:
    name: sendguard-agent
    state: restarted

- name: Restart sendguard-policyd
  ansible.builtin.systemd:
    name: sendguard-policyd
    state: restarted
